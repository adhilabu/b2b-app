[pytest]
asyncio_mode = auto
testpaths = .
python_files = test_*.py
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests requiring a live stack
